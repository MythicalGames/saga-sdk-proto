syntax = "proto3";

option java_multiple_files = true;
option java_package = "games.mythical.saga.sdk.proto.common";

package saga.proto.common.sort;

enum SortOrder {
    ASC = 0; // Sort ascending
    DESC = 1; // Sort descending
}
